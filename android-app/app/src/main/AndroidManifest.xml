<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 심야 앱 접속 빈도 측정: 설정 > 사용정보 접근에서 사용자가 직접 허용 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- 상시 상태바 표시용 포그라운드 서비스 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      의도적으로 선언하지 않는 권한:
      - SYSTEM_ALERT_WINDOW (다른 앱 위에 그리기): 충동 지연 화면은 오버레이가 아닌
        일반 Activity(전체화면 전환)로 구현해 악용 소지가 큰 권한을 배제한다.
    -->

    <application
        android:name=".DonghaengApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Donghaeng"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Donghaeng">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 충동 지연(Urge Surfing) 화면: 잠금화면 위 전체 화면 전환으로 노출 -->
        <activity
            android:name=".ui.urgesurfing.UrgeSurfingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Donghaeng.UrgeSurfing" />

        <!-- 상시 표시 포그라운드 서비스: 숨김 모드 없음 -->
        <service
            android:name=".service.MonitoringForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 지정된 금융 앱 알림만 온디바이스에서 파싱하는 리스너 -->
        <service
            android:name=".service.PaymentNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
